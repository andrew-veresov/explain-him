import { createPresentationArtifact, createSafeTextArtifact } from './presentation/artifact.mjs';

export const WORKSPACE_SCHEMA_VERSION = 'explain-him-local-workspace.v4';
export const LEGACY_WORKSPACE_SCHEMA_VERSION = 'explain-him-local-workspace.v1';
export const PREVIOUS_WORKSPACE_SCHEMA_VERSION = 'explain-him-local-workspace.v3';
export const OLDER_WORKSPACE_SCHEMA_VERSION = 'explain-him-local-workspace.v2';
export const ALLOWED_BLOCK_KINDS = Object.freeze(['example', 'analogy', 'summary', 'warning', 'comparison', 'diagram']);
const MAX_TRANSACTIONS = 250;
const clone = (value) => value === undefined ? undefined : JSON.parse(JSON.stringify(value));
function required(value, field, max = 500) { if (typeof value !== 'string' || !value.trim()) throw new TypeError(`${field} must be a non-empty string`); if (value.length > max) throw new RangeError(`${field} exceeds ${max} characters`); return value.trim(); }
function localId(prefix = 'presentation') { return `local-${prefix}-${globalThis.crypto?.randomUUID?.() || `${Date.now()}-${Math.random().toString(16).slice(2)}`}`; }
function transactionId() { return `local-transaction-${globalThis.crypto?.randomUUID?.() || Date.now()}`; }
function actorOf(actor, channel = 'browser-control') { return { kind: typeof actor?.kind === 'string' ? actor.kind.slice(0, 40) : 'agent', channel: typeof actor?.channel === 'string' ? actor.channel.slice(0, 40) : channel }; }

export function createInitialWorkspace({ explanationId, baseRevision }) { return { schemaVersion: WORKSPACE_SCHEMA_VERSION, explanationId: required(explanationId, 'explanationId', 160), baseRevision: required(baseRevision, 'baseRevision', 160), transactions: [], cursor: 0, revision: 0, viewMode: 'personalized' }; }
export function createAddPresentationOperation(input, options = {}) { const id = options.id || localId('presentation'); if (!/^local-[A-Za-z0-9._:-]+$/.test(id)) throw new TypeError('Local presentation id must start with local-'); const topicId = input.topicId === undefined ? null : required(input.topicId, 'topicId', 120); if (topicId !== null && !/^[A-Za-z][A-Za-z0-9._:-]*$/.test(topicId)) throw new TypeError('topicId must use the safe stable topic pattern'); return { type: 'add-presentation', presentation: { id, topicId, targetId: required(input.targetId, 'targetId', 120), placement: input.placement === 'replace' ? 'replace' : 'after', artifact: createPresentationArtifact(input.artifact || input), actor: actorOf(input.actor), createdAt: options.createdAt || new Date().toISOString(), updatedAt: options.updatedAt || options.createdAt || new Date().toISOString() } }; }
export function createUpdatePresentationOperation(presentationId, input) { const id = required(presentationId, 'presentationId', 120); if (!id.startsWith('local-')) throw new TypeError('Only local-* presentations can be updated'); return { type: 'update-presentation', presentationId: id, patch: { artifact: createPresentationArtifact(input.artifact || input), updatedAt: new Date().toISOString() } }; }
export function createRemovePresentationOperation(presentationId) { const id = required(presentationId, 'presentationId', 120); if (!id.startsWith('local-')) throw new TypeError('Only local-* presentations can be removed'); return { type: 'remove-presentation', presentationId: id }; }
export function createAddBlockOperation(input, options = {}) { const kind = required(input.kind, 'kind', 40); if (!ALLOWED_BLOCK_KINDS.includes(kind)) throw new TypeError(`Unsupported block kind: ${kind}`); const type = kind === 'comparison' ? 'comparison' : kind === 'diagram' ? 'diagram' : 'callout'; return createAddPresentationOperation({ targetId: input.targetId, placement: input.placement, artifact: createSafeTextArtifact({ type, title: input.title, body: input.body, provenance: input.provenance || { sourceBlockIds: [input.targetId], repositoryRefs: [] } }), actor: input.actor }, options); }
export const createRemoveBlockOperation = createRemovePresentationOperation;
function makeTransaction(operations, { id = transactionId(), requestId = null, requestFingerprint = null, semanticFingerprint = null, topicId = null, activationId = null, contractId = null, result = null, actor, createdAt = new Date().toISOString() } = {}) { if (!Array.isArray(operations) || !operations.length) throw new TypeError('transaction must contain operations'); return { id, requestId: requestId ? required(requestId, 'requestId', 160) : null, requestFingerprint: requestFingerprint ? required(requestFingerprint, 'requestFingerprint', 20000) : null, semanticFingerprint: semanticFingerprint ? required(semanticFingerprint, 'semanticFingerprint', 20000) : null, topicId: topicId ? required(topicId, 'topicId', 120) : null, activationId: activationId ? required(activationId, 'activationId', 160) : null, contractId: contractId ? required(contractId, 'contractId', 160) : null, result: result ? clone(result) : null, actor: actorOf(actor), createdAt, operations: clone(operations) }; }
export function appendTransaction(state, operations, options = {}) { if (!state || state.schemaVersion !== WORKSPACE_SCHEMA_VERSION) throw new TypeError('Unsupported workspace state'); const next = clone(state); next.transactions = next.transactions.slice(0, next.cursor); next.transactions.push(makeTransaction(operations, options)); if (next.transactions.length > MAX_TRANSACTIONS) next.transactions = next.transactions.slice(-MAX_TRANSACTIONS); next.cursor = next.transactions.length; next.revision += 1; return next; }
export function appendOperation(state, operation) { return appendTransaction(state, [operation]); }
export function undoWorkspace(state) { const next = clone(state); next.cursor = Math.max(0, next.cursor - 1); next.revision += 1; return next; }
export function redoWorkspace(state) { const next = clone(state); next.cursor = Math.min(next.transactions.length, next.cursor + 1); next.revision += 1; return next; }
export function setWorkspaceViewMode(state, viewMode) { if (!['original', 'personalized'].includes(viewMode)) throw new TypeError('viewMode must be original or personalized'); const next = clone(state); next.viewMode = viewMode; return next; }
function legacyBlockToPresentation(block) { return { id: block.id, targetId: block.targetId, placement: 'after', artifact: createSafeTextArtifact({ type: block.kind === 'comparison' ? 'comparison' : block.kind === 'diagram' ? 'diagram' : 'callout', title: block.title || 'Personal explanation', body: block.body || 'Imported local explanation', provenance: block.provenance || { sourceBlockIds: [block.targetId], repositoryRefs: [] } }), actor: actorOf(block.actor, 'migration'), createdAt: block.createdAt || new Date(0).toISOString(), updatedAt: block.createdAt || new Date(0).toISOString() }; }
function topicForLegacyPresentation(presentation) { return presentation.topicId || null; }
function migrateOperation(operation) { if (!operation || typeof operation !== 'object') return operation; if (operation.type === 'add-presentation' && operation.presentation) return { ...operation, presentation: { ...operation.presentation, topicId: topicForLegacyPresentation(operation.presentation) } }; return clone(operation); }
export function migrateWorkspaceState(state) { if (!state || typeof state !== 'object') return null; if (state.schemaVersion === WORKSPACE_SCHEMA_VERSION) return clone(state); if (state.schemaVersion === PREVIOUS_WORKSPACE_SCHEMA_VERSION) { const transactions = (state.transactions || []).map((transaction) => ({ ...clone(transaction), semanticFingerprint: transaction.semanticFingerprint || transaction.requestFingerprint || null, topicId: transaction.topicId || null, activationId: transaction.activationId || null, contractId: transaction.contractId || null, operations: (transaction.operations || []).map(migrateOperation) })); return { schemaVersion: WORKSPACE_SCHEMA_VERSION, explanationId: state.explanationId, baseRevision: state.baseRevision, transactions, cursor: Math.min(Number.isInteger(state.cursor) ? state.cursor : transactions.length, transactions.length), revision: Number.isInteger(state.revision) ? state.revision : 0, viewMode: state.viewMode === 'original' ? 'original' : 'personalized' }; } const oldOps = state.schemaVersion === LEGACY_WORKSPACE_SCHEMA_VERSION ? (state.operations || []).map((op) => op.type === 'add-block' ? { type: 'add-presentation', presentation: legacyBlockToPresentation(op.block) } : op.type === 'remove-block' ? { type: 'remove-presentation', presentationId: op.blockId } : null).filter(Boolean) : state.schemaVersion === OLDER_WORKSPACE_SCHEMA_VERSION ? (state.operations || []) : null; if (!oldOps) return null; const transactions = oldOps.map((operation, index) => makeTransaction([migrateOperation(operation)], { id: `local-migrated-${index + 1}`, actor: { kind: 'agent', channel: 'migration' }, createdAt: new Date(0).toISOString() })); return { schemaVersion: WORKSPACE_SCHEMA_VERSION, explanationId: state.explanationId, baseRevision: state.baseRevision, transactions, cursor: Math.min(Number.isInteger(state.cursor) ? state.cursor : transactions.length, transactions.length), revision: Number.isInteger(state.revision) ? state.revision : 0, viewMode: state.viewMode === 'original' ? 'original' : 'personalized' }; }
function activePresentations(state) { const active = new Map(); for (const transaction of state.transactions.slice(0, state.cursor)) for (const operation of transaction.operations) { if (operation.type === 'add-presentation') active.set(operation.presentation.id, clone(operation.presentation)); else if (operation.type === 'update-presentation' && active.has(operation.presentationId)) { const item = active.get(operation.presentationId); Object.assign(item, clone(operation.patch)); active.set(item.id, item); } else if (operation.type === 'remove-presentation') active.delete(operation.presentationId); } return [...active.values()]; }
export function materializeWorkspace(state, { canonicalIds = [], baseRevision } = {}) { const canonical = new Set(canonicalIds); const presentations = activePresentations(state).map((presentation) => ({ ...presentation, orphaned: !canonical.has(presentation.targetId) })); return { schemaVersion: state.schemaVersion, explanationId: state.explanationId, storedBaseRevision: state.baseRevision, currentBaseRevision: baseRevision || state.baseRevision, baseRevisionChanged: Boolean(baseRevision && baseRevision !== state.baseRevision), cursor: state.cursor, transactionCount: state.transactions.length, operationCount: state.transactions.reduce((n, item) => n + item.operations.length, 0), revision: state.revision, viewMode: state.viewMode, presentations, blocks: presentations.map((item) => ({ id: item.id, topicId: item.topicId, targetId: item.targetId, placement: item.placement, kind: item.artifact.type, title: item.artifact.fallback.title, body: item.artifact.fallback.body, actor: item.actor, provenance: item.artifact.provenance, createdAt: item.createdAt, updatedAt: item.updatedAt, orphaned: item.orphaned })), canUndo: state.cursor > 0, canRedo: state.cursor < state.transactions.length, orphanedPresentationCount: presentations.filter((item) => item.orphaned).length, orphanedBlockCount: presentations.filter((item) => item.orphaned).length }; }
export class MemoryWorkspaceStore { constructor() { this.mode = 'memory'; this.records = new Map(); } async load(key) { return clone(this.records.get(key) || null); } async save(key, value) { this.records.set(key, clone(value)); } async clear(key) { this.records.delete(key); } }
export class IndexedDbWorkspaceStore { constructor({ dbName = 'explain-him-public', storeName = 'workspaces' } = {}) { this.mode = 'indexeddb'; this.dbName = dbName; this.storeName = storeName; } async open() { return new Promise((resolve, reject) => { const request = globalThis.indexedDB.open(this.dbName, 1); request.onupgradeneeded = () => { if (!request.result.objectStoreNames.contains(this.storeName)) request.result.createObjectStore(this.storeName); }; request.onsuccess = () => resolve(request.result); request.onerror = () => reject(request.error); }); } async withStore(mode, action) { const db = await this.open(); try { return await new Promise((resolve, reject) => { const tx = db.transaction(this.storeName, mode); const request = action(tx.objectStore(this.storeName)); request.onsuccess = () => resolve(clone(request.result ?? null)); request.onerror = () => reject(request.error); }); } finally { db.close(); } } async load(key) { return this.withStore('readonly', (store) => store.get(key)); } async save(key, value) { await this.withStore('readwrite', (store) => store.put(clone(value), key)); } async clear(key) { await this.withStore('readwrite', (store) => store.delete(key)); } }
export function createWorkspaceStore() { return globalThis.indexedDB ? new IndexedDbWorkspaceStore() : new MemoryWorkspaceStore(); }
function make(document, tag, className, text) { const element = document.createElement(tag); if (className) element.className = className; if (text !== undefined) element.textContent = text; return element; }
function renderArtifactContent(document, article, artifact) { const payload = artifact.content?.payload || {}; const fallback = artifact.fallback; if (artifact.type === 'callout') { article.append(make(document, 'p', 'typed-callout-body', payload.body || fallback.body)); return; } if (artifact.type === 'comparison' && Array.isArray(payload.columns)) { const grid = make(document, 'div', 'typed-comparison-grid'); for (const column of payload.columns) { const card = make(document, 'section', 'typed-comparison-column'); card.append(make(document, 'h4', null, column.title)); const list = make(document, 'ul'); for (const item of column.items || []) list.append(make(document, 'li', null, item)); card.append(list); grid.append(card); } article.append(grid); return; } if (artifact.type === 'workflow' && Array.isArray(payload.steps)) { const list = make(document, 'ol', 'typed-workflow'); for (const step of payload.steps) { const item = make(document, 'li', 'typed-workflow-step'); item.append(make(document, 'strong', null, step.title)); if (step.body) item.append(make(document, 'p', null, step.body)); list.append(item); } article.append(list); return; } if (artifact.type === 'timeline' && Array.isArray(payload.items)) { const list = make(document, 'ol', 'typed-timeline'); for (const entry of payload.items) { const item = make(document, 'li', 'typed-timeline-item'); item.append(make(document, 'strong', 'typed-timeline-label', entry.label), make(document, 'p', null, entry.body)); list.append(item); } article.append(list); return; } if (artifact.type === 'diagram' && Array.isArray(payload.nodes)) { const diagram = make(document, 'div', 'typed-diagram'); const nodes = make(document, 'div', 'typed-diagram-nodes'); for (const node of payload.nodes) { const card = make(document, 'article', 'typed-diagram-node'); card.dataset.nodeId = node.id; card.append(make(document, 'strong', null, node.label)); if (node.body) card.append(make(document, 'small', null, node.body)); nodes.append(card); } diagram.append(nodes); const edges = make(document, 'ul', 'typed-diagram-edges'); for (const edge of payload.edges || []) edges.append(make(document, 'li', null, `${edge.from} → ${edge.to}${edge.label ? ` · ${edge.label}` : ''}`)); diagram.append(edges); article.append(diagram); return; } article.append(make(document, 'p', 'local-body', fallback.body)); }
function renderPresentation(document, presentation) { const artifact = presentation.artifact; const article = make(document, 'article', `local-explanation local-${artifact.type}`); article.dataset.ehLocalBlockId = presentation.id; article.dataset.ehLocalPresentationId = presentation.id; article.tabIndex = -1; article.setAttribute('aria-live', 'polite'); const header = make(document, 'header', 'local-explanation-header'); const heading = make(document, 'div'); heading.append(make(document, 'span', 'local-label', presentation.placement === 'replace' ? 'Personal version' : 'Personal presentation'), make(document, 'h3', null, artifact.fallback.title)); const remove = make(document, 'button', 'local-remove', 'Remove'); remove.type = 'button'; remove.dataset.ehRemoveLocal = presentation.id; header.append(heading, remove); article.append(header); renderArtifactContent(document, article, artifact); const meta = make(document, 'div', 'local-meta'); meta.append(make(document, 'span', null, artifact.type), make(document, 'span', null, presentation.placement)); article.append(meta); return article; }
export function renderWorkspace(document, view) { const slots = new Map(); for (const slot of document.querySelectorAll('[data-eh-local-slot]')) { slot.replaceChildren(); slots.set(slot.dataset.ehLocalSlot, slot); } for (const node of document.querySelectorAll('[data-eh-block-id]')) { node.hidden = false; node.removeAttribute('aria-hidden'); } if (view.viewMode === 'personalized') for (const presentation of view.presentations.filter((item) => item.placement === 'replace' && !item.orphaned)) { const target = document.querySelector(`[data-eh-block-id="${presentation.targetId}"]`); const slot = slots.get(presentation.targetId); if (target) { target.hidden = true; target.setAttribute('aria-hidden', 'true'); } if (slot) slot.append(renderPresentation(document, presentation)); } if (view.viewMode === 'personalized') for (const presentation of view.presentations.filter((item) => item.placement !== 'replace' && !item.orphaned)) slots.get(presentation.targetId)?.append(renderPresentation(document, presentation)); const summary = document.getElementById('workspace-summary'); if (summary) summary.textContent = `${view.viewMode === 'original' ? 'Original' : 'Personalized'} · ${view.presentations.length} saved local presentations`; const count = document.getElementById('workspace-count'); if (count) count.textContent = String(view.presentations.length); for (const button of document.querySelectorAll('[data-workspace-view]')) { const active = button.dataset.workspaceView === view.viewMode; button.setAttribute('aria-pressed', String(active)); } const undo = document.getElementById('workspace-undo'); if (undo) undo.disabled = !view.canUndo; const redo = document.getElementById('workspace-redo'); if (redo) redo.disabled = !view.canRedo; }
function clearFocused(document) { for (const node of document.querySelectorAll?.('.is-focused') || []) node.classList?.remove?.('is-focused'); }
export function focusAuthoredBlock(document, targetId) { const target = document.querySelector(`[data-eh-block-id="${targetId}"]`); if (!target) throw new RangeError(`Unknown authored target: ${targetId}`); const panel = target.closest?.('[data-section-panel]'); const sectionId = panel?.dataset?.sectionPanel; if (panel?.hidden && sectionId) { for (const section of document.querySelectorAll('[data-section-panel]')) { const active = section.dataset.sectionPanel === sectionId; section.hidden = !active; section.classList?.toggle?.('is-active', active); } for (const tab of document.querySelectorAll('[data-section]')) { const active = tab.dataset.section === sectionId; tab.classList?.toggle?.('is-active', active); tab.setAttribute?.('aria-selected', String(active)); if (active) tab.setAttribute?.('aria-current', 'page'); else tab.removeAttribute?.('aria-current'); } const currentFocus = document.getElementById?.('current-focus'); const activeTab = [...document.querySelectorAll('[data-section]')].find((tab) => tab.dataset.section === sectionId); if (currentFocus && activeTab) currentFocus.textContent = activeTab.textContent.trim(); } clearFocused(document); target.classList.add('is-focused'); target.scrollIntoView?.({ behavior: 'smooth', block: 'center' }); return { targetId }; }
export class ExplanationWorkspace { constructor({ document, store, explanationId, baseRevision, canonicalIds }) { this.document = document; this.store = store; this.explanationId = explanationId; this.baseRevision = baseRevision; this.canonicalIds = [...canonicalIds]; this.storageKey = `${explanationId}:${globalThis.location?.origin || 'local'}`; this.state = createInitialWorkspace({ explanationId, baseRevision }); } async initialize() { const loaded = await this.store.load(this.storageKey).catch(() => null); const migrated = migrateWorkspaceState(loaded); if (migrated?.explanationId === this.explanationId) { this.state = migrated; if (loaded?.schemaVersion !== WORKSPACE_SCHEMA_VERSION) await this.store.save(this.storageKey, this.state); } this.render(); return this; } view() { return materializeWorkspace(this.state, { canonicalIds: this.canonicalIds, baseRevision: this.baseRevision }); } render() { const view = this.view(); if (this.document) renderWorkspace(this.document, view); return view; } async persist() { await this.store.save(this.storageKey, this.state); return this.render(); } getContext() { return { explanationId: this.explanationId, baseRevision: this.baseRevision, workspaceRevision: this.state.revision, storageMode: this.store.mode, authoredTargetIds: [...this.canonicalIds], authoredLayerMutable: false, presentationSchemaVersion: 'explain-him-presentation.v1', capabilities: ['focus', 'add-local-presentation', 'replace-local-presentation', 'update-local-presentation', 'remove-local-presentation', 'undo', 'redo', 'original-view', 'export'] }; } getVisibleState() { return this.view(); } getLocalChangeHistory() { return { schemaVersion: this.state.schemaVersion, transactions: clone(this.state.transactions), cursor: this.state.cursor, revision: this.state.revision, canUndo: this.state.cursor > 0, canRedo: this.state.cursor < this.state.transactions.length }; } focusBlock({ targetId, blockId }) { const view = this.view(); const replacement = !blockId && view.viewMode === 'personalized' ? view.presentations.find((item) => item.targetId === targetId && item.placement === 'replace' && !item.orphaned) : null; const visibleBlockId = blockId || replacement?.id; if (visibleBlockId) { const node = this.document?.querySelector?.(`[data-eh-local-block-id="${visibleBlockId}"]`); if (node) { clearFocused(this.document); node.classList?.add?.('is-focused'); node.focus?.(); node.scrollIntoView?.({ behavior: 'smooth', block: 'center' }); return { targetId: targetId || replacement?.targetId, blockId: visibleBlockId, localPresentation: true }; } if (blockId) return { blockId, targetId: null, unavailable: true }; } return this.document ? focusAuthoredBlock(this.document, targetId) : { targetId }; } async applyTransaction(operations, options = {}) { this.state = appendTransaction(this.state, operations, options); return this.persist(); } async attachTransactionResult(transactionId, result) { const transaction = this.state.transactions.find((item) => item.id === transactionId); if (!transaction) throw new RangeError(`Unknown local transaction: ${transactionId}`); transaction.result = clone(result); await this.store.save(this.storageKey, this.state); return clone(transaction.result); } async addLocalPresentation(input) { if (!this.canonicalIds.includes(input.targetId)) throw new RangeError(`Unknown authored target: ${input.targetId}`); return this.applyTransaction([createAddPresentationOperation(input)], { actor: input.actor }); } async replaceLocalPresentation(input) { if (!this.canonicalIds.includes(input.targetId)) throw new RangeError(`Unknown authored target: ${input.targetId}`); if (this.view().presentations.some((item) => item.targetId === input.targetId && item.placement === 'replace')) throw new RangeError(`Target already has a local replacement: ${input.targetId}`); return this.applyTransaction([createAddPresentationOperation({ ...input, placement: 'replace' })], { actor: input.actor }); } async updateLocalPresentation({ presentationId, artifact, actor }) { if (!this.view().presentations.some((item) => item.id === presentationId)) throw new RangeError(`Unknown local presentation: ${presentationId}`); return this.applyTransaction([createUpdatePresentationOperation(presentationId, { artifact })], { actor }); } async removeLocalPresentation({ presentationId, actor }) { if (!this.view().presentations.some((item) => item.id === presentationId)) throw new RangeError(`Unknown local presentation: ${presentationId}`); return this.applyTransaction([createRemovePresentationOperation(presentationId)], { actor }); } async addLocalBlock(input) { return this.addLocalPresentation({ ...input, artifact: createAddBlockOperation(input).presentation.artifact }); } async removeLocalBlock({ blockId }) { return this.removeLocalPresentation({ presentationId: blockId }); } async undo() { this.state = undoWorkspace(this.state); return this.persist(); } async redo() { this.state = redoWorkspace(this.state); return this.persist(); } async setViewMode(viewMode) { this.state = setWorkspaceViewMode(this.state, viewMode); return this.persist(); } async reset({ confirmed } = {}) { if (confirmed !== true) throw new TypeError('Confirmed reset is required'); this.state = createInitialWorkspace({ explanationId: this.explanationId, baseRevision: this.baseRevision }); await this.store.clear(this.storageKey); return this.render(); } exportJson() { return JSON.stringify(this.state, null, 2); } }
export async function createExplanationWorkspace({ document, explanationId, baseRevision, canonicalIds, store = createWorkspaceStore() }) { return new ExplanationWorkspace({ document, store, explanationId, baseRevision, canonicalIds }).initialize(); }
